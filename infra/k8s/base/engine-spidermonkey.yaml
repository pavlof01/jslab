apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-spidermonkey
  labels:
    app: engine-spidermonkey
spec:
  replicas: 1
  # Single replica on a resource-tight node: recreate (no surge pod) so a
  # rollout never needs 2x capacity. Avoids old pods getting stuck terminating
  # when several engines roll at once. Trade-off: a few seconds of downtime
  # for this engine during its own deploy.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: engine-spidermonkey
  template:
    metadata:
      labels:
        app: engine-spidermonkey
    spec:
      terminationGracePeriodSeconds: 15
      # No egress allow reaches the cluster API anyway (see the NetworkPolicy);
      # this engine only compiles/disassembles and never executes user code,
      # but there's no legitimate use for the token here either.
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: engine-spidermonkey
          image: pavlof01/jslab-engine-spidermonkey:latest
          env:
            - name: NODE_ENV
              value: production
            - name: SM_PATH
              value: js
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
              ephemeral-storage: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
              ephemeral-storage: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: engine-spidermonkey
  labels:
    app: engine-spidermonkey
spec:
  selector:
    app: engine-spidermonkey
  ports:
    - port: 8080
      targetPort: 8080
