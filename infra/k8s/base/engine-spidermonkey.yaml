apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-spidermonkey
  labels:
    app: engine-spidermonkey
spec:
  replicas: 1
  # Single replica on a resource-tight node: recreate (no surge pod) so a
  # rollout never needs 2x capacity. Avoids old pods getting stuck terminating
  # when several engines roll at once. Trade-off: a few seconds of downtime
  # for this engine during its own deploy.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: engine-spidermonkey
  template:
    metadata:
      labels:
        app: engine-spidermonkey
    spec:
      terminationGracePeriodSeconds: 15
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: engine-spidermonkey
          image: pavlof01/jslab-engine-spidermonkey:latest
          env:
            - name: NODE_ENV
              value: production
            - name: SM_PATH
              value: js
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: engine-spidermonkey
  labels:
    app: engine-spidermonkey
spec:
  selector:
    app: engine-spidermonkey
  ports:
    - port: 8080
      targetPort: 8080
