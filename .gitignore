# Local editor/agent settings. Machine-specific absolute paths live in here,
# so it must never be shared — least of all from a public repository.
.claude/

# Test-run output
coverage/

# macOS Finder metadata
.DS_Store

# Installed dependencies
node_modules/

# Build output
dist/
.next/

# Environment files — keep secrets local, but track the example template
.env*
!.env.example
