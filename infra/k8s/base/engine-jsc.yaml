apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-jsc
  labels:
    app: engine-jsc
spec:
  replicas: 1
  # Single replica on a resource-tight node: recreate (no surge pod) so a
  # rollout never needs 2x capacity. Avoids old pods getting stuck terminating
  # when several engines roll at once. Trade-off: a few seconds of downtime
  # for this engine during its own deploy.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: engine-jsc
  template:
    metadata:
      labels:
        app: engine-jsc
    spec:
      terminationGracePeriodSeconds: 15
      # No egress allow reaches the cluster API anyway (see the NetworkPolicy),
      # but user code here can read the filesystem (see this service's own
      # lockdown comment in src/server.ts) — don't hand it a token it has no
      # legitimate use for.
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: engine-jsc
          image: pavlof01/jslab-engine-jsc:latest
          env:
            - name: NODE_ENV
              value: production
            - name: JSC_PATH
              value: jsc
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
              ephemeral-storage: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
              ephemeral-storage: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: engine-jsc
  labels:
    app: engine-jsc
spec:
  selector:
    app: engine-jsc
  ports:
    - port: 8080
      targetPort: 8080
